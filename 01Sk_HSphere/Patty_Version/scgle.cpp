#include "scgle.hpp"

scgle:: scgle()
{
  // Reading the options file
  // time_t t = time(NULL);
  // struct tm tm = *localtime(&t);
  // char directory[100];
  
  //  std::vector<std::string> lines = file.read_file_line_by_line_g("options.txt");
  
  // this->_kMax   = atof(lines[0].c_str());
  // this->_deltaK = atof(lines[1].c_str());
  // this->_phi    = atof(lines[2].c_str());
  // this->_tmp_i  = atof(lines[5].c_str());
  // this->_tmp_f  = atof(lines[6].c_str());
  // this->_kas = (int)(this->_kMax / this->_deltaK);
  // std::cout << "Kas = " << this->_kas << "\n";
  // file.set_date_FILE_from_Options_file();
  // std::cout <<"date = " << this->file.get_date() <<"\n";
  // sprintf(this->_date,"%f-%f %d-%d %d:%d:%d",this->_tmp_i, this->_tmp_f, tm.tm_mon+1, tm.tm_mday, tm.tm_hour, tm.tm_min, tm.tm_sec);
  // mkdir(this->_date, 0770);
  // sprintf(directory, "%s/efeself.dat",this->_date);
  
}

void
scgle::some_processing() {
  // std::vector<std::string> lines = file.read_file_line_by_line_g("options.txt");  
  // int num_lines = lines.size();
  
  // for (int i = 0; i < num_lines; ++i) {
  //   std::cout << lines[i] << "\n";
    
  // }

    
}
